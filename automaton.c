#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

char* apply_rule(const char *state, const int n_columns, const int idx, char *out) {
    strcpy(out, state);
    return 0;
}
